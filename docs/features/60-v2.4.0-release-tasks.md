# 📋 Tasks: v2.4.0 Release

> **Feature**: `60` — v2.4.0 Release
> **Architecture**: [`60-v2.4.0-release-architecture.md`](60-v2.4.0-release-architecture.md)
> **Date**: 2026-03-15

---

## Phase 1 — IMP-02: Consistent `config.Load()` (Do First)

- [ ] 1.1 Add `config.Load()` call at the top of `NewApp()` in `core/cli/root.go`
- [ ] 1.2 Keep the early `config.Load()` in `serve` command's `RunE` (needed before `NewApp()` for `RAPIDGO_MODE`) — the duplicate call in `NewApp()` is harmless (idempotent)
- [ ] 1.3 Remove `config.Load()` from `work` command's `RunE` in `core/cli/work.go` (now handled by `NewApp()`)
- [ ] 1.4 Remove `config.Load()` from `schedule:run` command's `RunE` in `core/cli/schedule_run.go` (now handled by `NewApp()`)

### Checkpoint 1
- [ ] `serve` still reads `RAPIDGO_MODE` correctly
- [ ] `migrate`, `db:seed` now read `.env` values (DB_HOST, etc.)
- [ ] All existing tests pass

---

## Phase 2 — BUG-02: Fix `serveSingle()` Route Registration

- [ ] 2.1 Add `applyRoutesForMode(r, application.Container, mode)` call in `serveSingle()` after getting the router from the container
- [ ] 2.2 Verify `routeRegistrar` fires for single-port serve

### Checkpoint 2
- [ ] `SetRoutes()` callback fires in single-port mode
- [ ] Health routes register in single-port mode
- [ ] Template/static loading happens in single-port mode

---

## Phase 3 — BUG-03: Fix `serveMulti()` Provider Middleware

- [ ] 3.1 Add `GlobalHandlers()` method to `Router` in `core/router/router.go`
- [ ] 3.2 Update `serveMulti()` to get the container's router
- [ ] 3.3 Copy global handlers from container router to each per-service router before calling `applyRoutesForMode()`

### Checkpoint 3
- [ ] Provider-registered global middleware applies to all per-service routers
- [ ] Per-service routers still get correct mode-specific routes
- [ ] Multi-port mode works with separate ports

---

## Phase 4 — IMP-01: Configurable Server Timeouts

- [ ] 4.1 Add `parseDuration()` helper in `core/cli/serve.go`
- [ ] 4.2 Add `resolveServerTimeouts()` helper returning read, write, idle, shutdown durations
- [ ] 4.3 Update `serveSingle()` to use `resolveServerTimeouts()`
- [ ] 4.4 Update `serveMulti()` to use `resolveServerTimeouts()`
- [ ] 4.5 Add `parseDuration` unit test in `core/cli/serve_test.go`

### Checkpoint 4
- [ ] Default behavior unchanged (same values as current hardcoded)
- [ ] Setting `SERVER_READ_TIMEOUT=30s` changes read timeout
- [ ] Invalid duration strings fall back to defaults

---

## Phase 5 — FEAT-04: Router `NoRoute()`

- [ ] 5.1 Add `NoRoute(handlers ...gin.HandlerFunc)` method to `Router` in `core/router/router.go`
- [ ] 5.2 Add unit test in `core/router/router_test.go`

### Checkpoint 5
- [ ] `r.NoRoute(handler)` works without accessing `r.Engine()` directly

---

## Phase 6 — FEAT-03: Health Endpoint with Version

- [ ] 6.1 Update `health.Routes()` signature to accept optional `version ...string`
- [ ] 6.2 Include version in `/health` response when provided
- [ ] 6.3 Update call site in `applyRoutesForMode()` to pass `cli.Version`
- [ ] 6.4 Add unit test for health with and without version

### Checkpoint 6
- [ ] `GET /health` with version returns `{"status":"ok","version":"0.2.0"}`
- [ ] `GET /health` without version returns `{"status":"ok"}` (backward compat)
- [ ] `GET /health/ready` unchanged

---

## Phase 7 — Final

- [ ] 7.1 Run full test suite
- [ ] 7.2 Update `core/cli/root.go` Version constant to `"2.4.0"`
- [ ] 7.3 Update changelog
- [ ] 7.4 Commit and tag `v2.4.0`
