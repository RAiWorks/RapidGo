# 📋 Tasks: v2.5.0 Release

> **Feature**: `61` — v2.5.0 Release
> **Architecture**: [`61-v2.5.0-release-architecture.md`](61-v2.5.0-release-architecture.md)
> **Date**: 2026-03-15

---

## Phase 1 — ENH-08: Expanded Error Codes

- [ ] 1.1 Rename `AppError.Code int` → `AppError.Status int` in `core/errors/errors.go`
- [ ] 1.2 Add `Code string` field to `AppError`
- [ ] 1.3 Add `WithCode(code string) *AppError` builder method
- [ ] 1.4 Add `HTTPStatus() int` deprecated helper method
- [ ] 1.5 Update all 7 factory functions (`NotFound`, `BadRequest`, `Internal`, `Unauthorized`, `Forbidden`, `Conflict`, `Unprocessable`) to set both `Status` and default `Code`
- [ ] 1.6 Update `ErrorResponse()` to include `code` in response
- [ ] 1.7 Update error handler middleware (`core/middleware/error_handler.go` lines 22, 27) to use `Status` instead of `Code`
- [ ] 1.8 Update `core/errors/errors_test.go` references (`appErr.Code` → `appErr.Status` at lines 49-50, 82-83)
- [ ] 1.9 Search and update any other internal framework references to `AppError.Code` (now `Status`)
- [ ] 1.10 Add/update tests in `core/errors/errors_test.go` for new `Code string` field and `WithCode()`

### Checkpoint 1
- [ ] All factories return correct `Status` and `Code`
- [ ] `WithCode()` overrides default code
- [ ] `ErrorResponse()` includes `"code"` field
- [ ] All internal references compile

---

## Phase 2 — ENH-09: Rate Limit Convenience Helpers

- [ ] 2.1 Add `KeyByIP()` function in `core/middleware/ratelimit.go`
- [ ] 2.2 Add `KeyByUserID(contextKey string)` function
- [ ] 2.3 Add `KeyByHeader(header string)` function
- [ ] 2.4 Add `KeyByBodyField(field string)` function
- [ ] 2.5 Add `ParseRate(rate string)` function
- [ ] 2.6 Add tests for all helpers in `core/middleware/ratelimit_test.go`

### Checkpoint 2
- [ ] All key functions return correct keys
- [ ] Fallback to IP when key source is missing
- [ ] `ParseRate()` validates format correctly

---

## Phase 3 — IMP-06: `container.TryMake()`

- [ ] 3.1 Add `TryMake(name string) (interface{}, error)` to `Container`
- [ ] 3.2 Add generic `TryMake[T any](c *Container, name string) (T, error)` function
- [ ] 3.3 Add tests in `core/container/container_test.go`

### Checkpoint 3
- [ ] `TryMake` returns error for missing service (no panic)
- [ ] `TryMake` returns value for registered service
- [ ] Generic `TryMake[T]` catches type assertion failures

---

## Phase 4 — FEAT-01: Local Mode Helper

- [ ] 4.1 Add `IsLocal() bool` to `core/config/environment.go` (alongside `IsProduction()`, `IsDevelopment()`, `IsTesting()`)
- [ ] 4.2 Add test for `IsLocal()` in `core/config/environment_test.go`

### Checkpoint 4
- [ ] `IsLocal()` returns true for "local" and "development"
- [ ] `IsLocal()` returns false for "production", "testing"

---

## Phase 5 — IMP-05: `migrate:fresh` Command

- [ ] 5.1 Create `core/cli/migrate_fresh.go`
- [ ] 5.2 Implement table drop + re-migration logic
- [ ] 5.3 Add `--force` flag requirement for production
- [ ] 5.4 Register command in `root.go`
- [ ] 5.5 Add test for production guard

### Checkpoint 5
- [ ] `migrate:fresh` drops and recreates all tables
- [ ] Refuses to run in production without `--force`

---

## Phase 6 — FEAT-02: `db:wipe` Command

- [ ] 6.1 Create `core/cli/db_wipe.go`
- [ ] 6.2 Implement table truncation with dialect awareness (PostgreSQL CASCADE, MySQL, SQLite)
- [ ] 6.3 Skip `migrations` tracking table
- [ ] 6.4 Add `--force` flag requirement for production
- [ ] 6.5 Register command in `root.go`
- [ ] 6.6 Add test for production guard

### Checkpoint 6
- [ ] `db:wipe` truncates all user tables
- [ ] Preserves `migrations` table
- [ ] Refuses to run in production without `--force`

---

## Phase 7 — IMP-03 + IMP-04: Seeder Improvements

- [ ] 7.1 Improve nil seeder error message in `core/cli/seed.go`
- [ ] 7.2 Add `SeederListFunc` type and `SetSeederList()` to `core/cli/hooks.go`
- [ ] 7.3 Add `--list` flag to `db:seed` command
- [ ] 7.4 Implement list output when `--list` is passed

### Checkpoint 7
- [ ] `db:seed --list` shows registered seeders
- [ ] Clear error when no seeder function registered

---

## Phase 8 — FEAT-05: `make:seeder` Scaffold

- [ ] 8.1 Add `seederTpl` template to `core/cli/make_scaffold.go`
- [ ] 8.2 Add `makeSeederCmd` command
- [ ] 8.3 Register command in `root.go`
- [ ] 8.4 Add test that scaffold generates correct file

### Checkpoint 8
- [ ] `make:seeder UserSeeder` creates `database/seeders/user_seeder.go`
- [ ] Generated file compiles

---

## Phase 9 — FEAT-06: Configurable Banner

- [ ] 9.1 Extract banner printing to `printBanner()` function in `core/cli/serve.go`
- [ ] 9.2 Support `APP_BANNER` env var override
- [ ] 9.3 Update both single-port and multi-port banner printing to use `printBanner()`

### Checkpoint 9
- [ ] Default banner unchanged
- [ ] Setting `APP_BANNER` replaces default output

---

## Phase 10 — Final

- [ ] 10.1 Run full test suite
- [ ] 10.2 Update `core/cli/root.go` Version constant to `"2.5.0"`
- [ ] 10.3 Update changelog
- [ ] 10.4 Commit and tag `v2.5.0`
