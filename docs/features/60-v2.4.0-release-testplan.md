# 🧪 Test Plan: v2.4.0 Release

> **Feature**: `60` — v2.4.0 Release
> **Date**: 2026-03-15

---

## Test Strategy

Unit tests per change. Integration-level verification via existing test infrastructure. No database required for most tests.

---

## Test Cases

### IMP-02: Consistent `config.Load()`

| # | Test | Expected |
|---|---|---|
| T-001 | `NewApp()` loads `.env` values | `config.Env("APP_NAME")` returns value from `.env` |
| T-002 | `config.Load()` safe to call multiple times | No panic on double-load (idempotent) |

### BUG-02: `serveSingle()` Route Registration

| # | Test | Expected |
|---|---|---|
| T-003 | `SetRoutes()` callback fires in single-port mode | Routes registered via `routeRegistrar` are accessible |
| T-004 | Health routes register in single-port mode | `GET /health` returns 200 |
| T-005 | Template loading triggers in single-port web mode | `resources/views/` loaded when present |

### BUG-03: `serveMulti()` Provider Middleware

| # | Test | Expected |
|---|---|---|
| T-006 | `GlobalHandlers()` returns registered global middleware | Non-empty slice after `r.Use()` |
| T-007 | Per-service routers inherit global middleware | Middleware from container router applies to per-service routers |

### IMP-01: Configurable Server Timeouts

| # | Test | Expected |
|---|---|---|
| T-008 | `parseDuration("30s", default)` | Returns 30s |
| T-009 | `parseDuration("invalid", 15s)` | Returns 15s fallback |
| T-010 | `parseDuration("", 15s)` | Returns 15s fallback |
| T-011 | `resolveServerTimeouts()` with no env vars | Returns defaults (15s, 15s, 60s, 30s) |
| T-012 | `resolveServerTimeouts()` with `SERVER_READ_TIMEOUT=30s` | Returns 30s for read, defaults for rest |

### FEAT-04: Router `NoRoute()`

| # | Test | Expected |
|---|---|---|
| T-013 | `r.NoRoute(handler)` registers handler | Unknown path returns custom response, not Gin default 404 |

### FEAT-03: Health with Version

| # | Test | Expected |
|---|---|---|
| T-014 | `health.Routes(r, dbFn, "2.4.0")` | `GET /health` returns `{"status":"ok","version":"2.4.0"}` |
| T-015 | `health.Routes(r, dbFn)` (no version) | `GET /health` returns `{"status":"ok"}` (backward compat) |
| T-016 | `GET /health/ready` with version arg | Response unchanged — still checks DB only |

---

## Regression

- [ ] All existing tests pass (`go test ./...`)
- [ ] Single-port serve starts without error
- [ ] Multi-port serve starts without error
- [ ] `migrate` command works with `.env` DB config
- [ ] `db:seed` command works with `.env` DB config
