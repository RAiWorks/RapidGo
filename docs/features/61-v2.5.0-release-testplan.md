# 🧪 Test Plan: v2.5.0 Release

> **Feature**: `61` — v2.5.0 Release
> **Date**: 2026-03-15

---

## Test Strategy

Unit tests per change. Destructive commands (`migrate:fresh`, `db:wipe`) tested for production safety guards. Error system tests verify both field values and JSON response format.

---

## Test Cases

### ENH-08: Expanded Error Codes

| # | Test | Expected |
|---|---|---|
| T-001 | `NotFound("not found")` | `Status: 404`, `Code: "NOT_FOUND"`, `Message: "not found"` |
| T-002 | `BadRequest("bad")` | `Status: 400`, `Code: "BAD_REQUEST"` |
| T-003 | `Internal(err)` | `Status: 500`, `Code: "INTERNAL_ERROR"` |
| T-004 | `Unauthorized("no")` | `Status: 401`, `Code: "UNAUTHORIZED"` |
| T-005 | `Forbidden("no")` | `Status: 403`, `Code: "FORBIDDEN"` |
| T-006 | `Conflict("dup")` | `Status: 409`, `Code: "CONFLICT"` |
| T-007 | `Unprocessable("bad")` | `Status: 422`, `Code: "UNPROCESSABLE"` |
| T-008 | `BadRequest("x").WithCode("INVALID_EMAIL")` | `Code: "INVALID_EMAIL"` overrides default |
| T-009 | `ErrorResponse()` format | Contains `"code"`, `"error"`, `"success"` keys |
| T-010 | `HTTPStatus()` method | Returns same value as `.Status` field |

### ENH-09: Rate Limit Helpers

| # | Test | Expected |
|---|---|---|
| T-011 | `KeyByIP()` returns client IP | Matches `c.ClientIP()` |
| T-012 | `KeyByUserID("userID")` with context value | Returns `"user:<value>"` |
| T-013 | `KeyByUserID("userID")` without context value | Falls back to IP |
| T-014 | `KeyByHeader("X-API-Key")` with header | Returns `"header:X-API-Key:<value>"` |
| T-015 | `KeyByHeader("X-API-Key")` without header | Falls back to IP |
| T-016 | `ParseRate("100-M")` | Valid rate, no error |
| T-017 | `ParseRate("invalid")` | Returns error |

### IMP-06: `container.TryMake()`

| # | Test | Expected |
|---|---|---|
| T-018 | `TryMake("missing")` | Returns nil, error (no panic) |
| T-019 | `TryMake("registered")` | Returns value, nil |
| T-020 | `TryMake[string](c, "intService")` | Returns zero, type assertion error |
| T-021 | `TryMake[*gorm.DB](c, "db")` | Returns DB instance, nil |

### FEAT-01: `IsLocal()`

| # | Test | Expected |
|---|---|---|
| T-022 | `APP_ENV=local` → `IsLocal()` | `true` |
| T-023 | `APP_ENV=development` → `IsLocal()` | `true` |
| T-024 | `APP_ENV=production` → `IsLocal()` | `false` |
| T-025 | `APP_ENV=testing` → `IsLocal()` | `false` |

### IMP-05: `migrate:fresh`

| # | Test | Expected |
|---|---|---|
| T-026 | Run in `production` without `--force` | Error: "refusing to run" |
| T-027 | Run in `development` | Drops and recreates tables |
| T-028 | Run in `production --force` | Executes (with warning) |

### FEAT-02: `db:wipe`

| # | Test | Expected |
|---|---|---|
| T-029 | Run in `production` without `--force` | Error: "refusing to wipe" |
| T-030 | Run in `development` | Truncates all user tables |
| T-031 | `migrations` table preserved after wipe | Migration tracking intact |

### IMP-03 + IMP-04: Seeder Improvements

| # | Test | Expected |
|---|---|---|
| T-032 | `db:seed` with no seeder registered | Clear error message with hint |
| T-033 | `db:seed --list` with seeder list registered | Prints seeder names |
| T-034 | `db:seed --list` with no list registered | Prints hint message |

### FEAT-05: `make:seeder`

| # | Test | Expected |
|---|---|---|
| T-035 | `make:seeder UserSeeder` | Creates `database/seeders/user_seeder.go` |
| T-036 | Generated file compiles | Valid Go syntax |
| T-037 | Duplicate name | Error: file already exists |

### FEAT-06: Configurable Banner

| # | Test | Expected |
|---|---|---|
| T-038 | No `APP_BANNER` set | Default banner displays |
| T-039 | `APP_BANNER` set | Custom banner replaces default |

---

## Regression

- [ ] All existing tests pass (`go test ./...`)
- [ ] Error handler middleware uses `Status` field correctly
- [ ] Existing `RateLimitMiddleware()` and `RateLimitWithConfig()` still work
- [ ] `Make()` still panics (unchanged behavior)
- [ ] All scaffold commands still work
