# ✅ Tasks: v2.7.0 Release

> **Feature**: `63` — v2.7.0 Release
> **Architecture**: [`architecture.md`](63-v2.7.0-release-architecture.md)
> **Test Plan**: [`testplan.md`](63-v2.7.0-release-testplan.md)
> **Changelog**: [`changelog.md`](63-v2.7.0-release-changelog.md)

---

## Phase 1 — ENH-02: UUIDBaseModel

- [x] 1.1 — Add `UUIDBaseModel` struct to `database/models/base.go` with `ID string`, `CreatedAt`, `UpdatedAt`, `DeletedAt`
- [x] 1.2 — Add `BeforeCreate` GORM hook that generates UUID v4 if ID is empty
- [x] 1.3 — Add import for `github.com/google/uuid`
- [x] 1.4 — Add tests: TC-U01 (fields check), TC-U02 (embed inherits ID), TC-U03 (AutoMigrate), TC-U04 (BeforeCreate auto-generates UUID), TC-U05 (BeforeCreate preserves pre-set ID)

### Checkpoint 1
- [x] `go build ./database/models/...` passes
- [x] `go test ./database/models/...` passes

---

## Phase 2 — ENH-07: make:module Command

- [x] 2.1 — Add `makeModuleCmd` to `core/cli/make_scaffold.go` with 4 templates (models, service, controller, routes)
- [x] 2.2 — Register `makeModuleCmd` in `core/cli/root.go`
- [x] 2.3 — Add test: TC-M01 (make:module creates 4 files in correct directory)

### Checkpoint 2
- [x] `go build ./core/cli/...` passes
- [x] `go test ./core/cli/...` passes

---

## Phase 3 — Finalize

- [x] 3.1 — Bump version to "2.7.0" in `core/cli/root.go`
- [x] 3.2 — Run full test suite: `go test -count=1 ./...`
- [x] 3.3 — Run `go build ./...`
- [x] 3.4 — Update changelog.md with build session details
- [x] 3.5 — Commit: `feat(v2.7.0): UUIDBaseModel, make:module scaffold`
